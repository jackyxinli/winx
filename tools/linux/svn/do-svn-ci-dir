if [ -z $1 ]; then
	exit 1
fi

Arg=$1

if [ -d $Arg ]; then
	mv $Arg $Arg.svn-ci-temp
	svn mkdir $Arg
	svn ci $Arg -m "mkdir $Arg"
	mv $Arg.svn-ci-temp/* $Arg
	rmdir $Arg.svn-ci-temp

	AppPath=`dirname $0`
	echo "" > svn-ci-dir.temp
fi

