AppPath=`dirname $0`
while :
do
	rm -f svn-ci-dir.temp
	find . -type d -name '*.svn-ci-temp' | xargs rm -rf
	svn status | grep -e '^?.*' | sed -e 's|. *\(.*\)|\1|' | xargs --no-run-if-empty -l1 $AppPath/do-svn-ci-dir
	if [ ! -f svn-ci-dir.temp ]; then
		break
	fi
done

